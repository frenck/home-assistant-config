---
# The MQTT integration.
#
# This integration can be managed via the UI, however, in YAML
# there are a few more advanced settings available.
#
# https://www.home-assistant.io/integrations/mqtt/
#
mqtt:
  # Set a client ID, to better recognize this client on the broker.
  client_id: home-assistant-prod
